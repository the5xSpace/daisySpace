[**daisy-space-sdk v0.3.0**](../README.md)

***

[daisy-space-sdk](../README.md) / formatDateTime

# Function: formatDateTime()

> **formatDateTime**(`dateTime`, `pattern`): `string`

Formats a date-time context using token patterns.

Supports `YYYY`, `MMM`, `MM`, `M`, `DD`, `D`, `HH`, `H`, `mm`, `m`,
`ss`, `s`, `SSS`, `TZ`, and `Z`.

## Parameters

### dateTime

[`TimeFormatDateTime`](../interfaces/TimeFormatDateTime.md)

### pattern

`string`

## Returns

`string`
