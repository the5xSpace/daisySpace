api/classes/index.md|共 234 个 classes。|234 classes total.| | 名称 | 链接 | | Name | Link |
api/classes/ModelNodeTransform.md|节点变换控制器（链式调用）。|Node transform controller (chainable).|通过 ModelFeature.transformNode(name) 获取实例；变换会作为状态副作用在每帧 update 自动生效。|Obtain an instance via ModelFeature.transformNode(name). Transforms are applied automatically as state side effects in each frame's update.
api/enums/index.md|共 27 个 enums。|27 enums total.| | 名称 | 链接 | | Name | Link |
api/functions/classifySunOcclusion.md|根据观察点看到的光源与遮挡天体视圆关系判定光照状态。|Determines the illumination state based on the apparent disk relationship between the light source and the occluding celestial body as seen from the observation point.|当遮挡天体视圆完整覆盖光源时返回 `umbra`，部分相交时返回 `penumbra`，|Returns `umbra` when the occluding body's disk fully covers the light source, `penumbra` when partially intersecting,|否则返回 `sunlit`。|and `sunlit` otherwise.
api/functions/createDisposableDetachMaterial.md|创建一个组件移除时使用的临时材质。|Creates a temporary material used when removing a component.|线对象从集合移除时会销毁自身挂载的材质。Feature 销毁只应解除组件对业务材质的引用，|When a line object is removed from a collection, its attached material is destroyed. Feature destruction should only release the component's reference to the business material,|不能销毁外部传入或多处共享的材质。|not destroy externally provided or shared materials.
api/functions/encodePolygons.md|将所有 CoveragePolygon 绘制到一张 canvas 上。|Draws all CoveragePolygons onto a single canvas.|每个 ring 会先做经线解包，再展开成一个或多个独立的 canvas path，|Each ring is first unwrapped along the meridian, then expanded into one or more independent canvas paths,|这样就不会把跨 ±180° 的边硬连成一条横贯全图的长边。|so that edges crossing ±180° are not forced into a single line spanning the entire map.
api/functions/formatDateTime.md|使用 token 模式格式化日期时间上下文。|Formats a date-time context using token patterns.|支持 `YYYY`、`MMM`、`MM`、`M`、`DD`、`D`、`HH`、`H`、`mm`、`m`、|Supports `YYYY`, `MMM`, `MM`, `M`, `DD`, `D`, `HH`, `H`, `mm`, `m`,|`ss`、`s`、`SSS`、`TZ` 和 `Z`。|`ss`, `s`, `SSS`, `TZ`, and `Z`.
api/functions/hslToRgb.md|HSL 转 RGB。|HSL to RGB.|- h: 角度制 0~360|- h: 0~360 in degrees
api/functions/index.md|共 86 个 functions。|86 functions total.| | 名称 | 链接 | | Name | Link |
api/functions/mergeTimeFormatOptions.md|合并基础与局部时间格式配置。|Merges base and local time format configurations.|当局部配置是预设名或回调时会完整替换基础配置；局部配置为对象时按字段覆盖。|When the local config is a preset name or callback, it fully replaces the base config. When the local config is an object, it merges by field.
api/functions/rgbToHsl.md|RGB 转 HSL。|RGB to HSL.|- 输入与输出分量均为 0~1，h 为角度制 0~360|- Input and output components are 0~1, h is 0~360 in degrees
api/interfaces/BodyTrackedCameraState.md|机体跟踪相机的局部状态。|Local state of the body-tracked camera.|将相机位置与姿态表示在"机体坐标系"（随天体自转的局部坐标系）中，便于在天体旋转时保持相对视角稳定。|Represents camera position and orientation in the "body coordinate system" (a local coordinate system that rotates with the celestial body), making it easy to maintain a stable relative view as the celestial body rotates.
api/interfaces/DynamicShaderParamOptions.md|自定义动态 Shader 参数配置。|Custom dynamic shader parameter configuration.|解析器返回 `undefined` 时使用的值，同时用于校验声明的 GLSL 类型。|The value used when the parser returns `undefined`, also used to validate the declared GLSL type.
api/interfaces/index.md|共 129 个 interfaces。|129 interfaces total.| | 名称 | 链接 | | Name | Link |
api/interfaces/UI.PopoverVisibilityChangeEvent.md|当前帧 DOM 是否真实显示；距离、遮挡、离屏可能让它为 false。|Whether the DOM is actually visible in the current frame; distance, occlusion, and off-screen conditions may cause it to be false.|由触发器/API 决定的"请求显示"状态。|The "requested display" state determined by the trigger or API.
api/modules/index.md|共 23 个 modules。|23 modules total.| | 名称 | 链接 | | Name | Link |
api/types/ArcRotateInputInstallOptions.md|输入监听安装参数。|Input listener installation parameters.|- canvas：接收 pointer/wheel 事件|- canvas: receives pointer/wheel events|- screenSpaceCameraController：可选，用于在安装时禁用输入、卸载时恢复|- screenSpaceCameraController: optional, used to disable input on install and restore on uninstall
api/types/ArcRotateTargetFrameMode.md|环绕相机跟随目标时使用的参考系。|Reference frame used by the orbit camera when following a target.|- model：使用目标 worldMatrix，环绕视角会继承目标姿态|- model: uses the target's worldMatrix, the orbit view inherits the target's orientation|- enu：使用目标位置的 East-North-Up 参考系，避免相机继承目标俯仰/滚转|- enu: uses the East-North-Up reference frame at the target position, preventing the camera from inheriting the target's pitch/roll
api/types/ArrowPointerLabelOptions.md|箭头前端 label 配置。|Arrow tip label configuration.|该类型在 `DaisyLabelOptions` 的基础上要求必须提供 `text`。|This type extends `DaisyLabelOptions` and requires that `text` be provided.
api/types/ArrowPointerTargetCallback.md|ArrowPointerFeature 可用的指向目标类型。|Target types available for ArrowPointerFeature.|- `Daisy.Cartesian3`：静态坐标点|- `Daisy.Cartesian3`: static coordinate point|- `Entity`：Daisy 实体（内部会转成回调，每帧取其当前位置）|- `Entity`: Daisy entity (internally converted to a callback, gets its current position each frame)|- `Entity`： 实体（内部会转成回调，每帧用 `position.getValue(time)` / `getValueInReferenceFrame`）|- `Entity`: entity (internally converted to a callback, uses `position.getValue(time)` / `getValueInReferenceFrame` each frame)|- `ArrowPointerTargetCallback`：回调函数（由使用方自行计算目标位置）|- `ArrowPointerTargetCallback`: callback function (the caller computes the target position)
api/types/AutoNormalsMode.md|法线自动计算模式。|Normal auto-computation mode.|- "flat"：每个顶点的法线 = 其所属三角形的面法线，棱角分明。|- "flat": each vertex normal equals its triangle's face normal, giving sharp edges.|- "smooth"：每个顶点的法线 = 其相邻三角形面法线的平均，光滑曲面。|- "smooth": each vertex normal is the average of its adjacent triangle face normals, giving smooth surfaces.|- false：不自动计算，使用用户传入的法线。|- false: do not auto-compute, use user-provided normals.
api/types/CollisionHandlerFunc.md|碰撞处理回调函数类型|Collision handler callback function type|被碰撞的盒子，可以通过 box.entity 获取实体对象|The collided box; the entity object can be accessed via box.entity
api/types/ConjunctionEvent.md|交汇事件（>=2 星最近距离预报）。|Conjunction event (closest approach prediction for >=2 satellites).|- `tcaUtc`: 最近距离发生时刻（TCA）|- `tcaUtc`: Time of Closest Approach (TCA)|- `minDistance`: 最近距离（m）|- `minDistance`: minimum distance (m)|- `relativeSpeedPerSecond`: 相对速度模长（m/s），基于 ECEF 速度估计|- `relativeSpeedPerSecond`: relative speed magnitude (m/s), estimated from ECEF velocities|- `stateA`/`stateB`: 同一 `tcaUtc` 下双方的完整状态输出（含 ECI/ECEF/LLA）|- `stateA`/`stateB`: full state output for both objects at the same `tcaUtc` (including ECI/ECEF/LLA)
api/types/ConjunctionOptions.md|交汇预报选项。|Conjunction prediction options.|- `stepSeconds`: 采样步长（越小越精细但更耗时）|- `stepSeconds`: sampling step size (smaller = more precise but slower)|- `distanceThreshold`: 触发候选区间的距离阈值（只对阈值内区间做细化搜索）|- `distanceThreshold`: distance threshold for triggering candidate intervals (only intervals within the threshold are refined)
api/types/EphemerisDataCallbackOptions.md|星历数据源配置（回调追加形式）。|Ephemeris data source configuration (callback append form).|用于持续追加位置点（如实时流数据）。|Used for continuously appending position points (e.g., real-time streaming data).
api/types/FeatureEventHandleHost.md|FeatureEventHandle 的宿主能力集合。|FeatureEventHandle's host capability set.|由 Feature 基类实现，用于把 ViewerEventHandle 的 SPACE_ENTITY_* 事件|Implemented by the Feature base class to map ViewerEventHandle's SPACE_ENTITY_* events|映射为"Feature 自身可监听 + 可选向上提交 Entity"的统一交互事件系统。|into a unified interaction event system where "Feature can listen itself + optionally submit Entity upward".
api/types/Holes.md|多边形孔洞结构（positions + holes）。|Polygon hole structure (positions + holes).|与 Daisy.PolygonHierarchy 的概念一致，可递归嵌套。|Consistent with the concept of Daisy.PolygonHierarchy, allowing recursive nesting.
api/types/index.md|共 266 个 types。|266 types total.| | 名称 | 链接 | | Name | Link |
api/types/InterSatelliteVisibilityOptions.md|互视窗口预报选项。|Inter-satellite visibility window prediction options.|- `stepSeconds`: 采样步长（越小越精细但更耗时）|- `stepSeconds`: sampling step size (smaller = more precise but slower)
api/types/KeplerElementDataOptions.md|开普勒根数数据源配置。|Keplerian element data source configuration.|数值单位与含义取决于上游数据约定，SDK 会按字段名直接使用。|The units and meaning of values depend on upstream data conventions; the SDK uses them directly by field name.
api/types/ModelNodeTransformState.md|模型节点变换状态（状态副作用）。|Model node transform state (state side effect).|- 该状态不会立即修改节点渲染，而是在 ModelFeature.update() 每帧自动应用到对应节点|- This state does not immediately modify node rendering; it is automatically applied to the corresponding node in each frame's ModelFeature.update()|- matrix 优先级最高：|- matrix has highest priority:|- matrix === null：恢复为 undefined，把变换控制权交还给 glTF 原始 transform/动画|- matrix === null: restores to undefined, returning transform control to the glTF original transform/animation|- matrix 存在：直接覆盖节点 matrix|- matrix present: directly overwrites the node matrix|- 底层 ModelNode 本身不提供节点级 color/alpha|- The underlying ModelNode does not provide node-level color/alpha|- Daisy 在内部通过修改模型 DrawCommand 的 uniformMap，让节点复用 的 model_color/model_colorBlend 管线|- Daisy internally modifies the model's DrawCommand uniformMap, allowing the node to reuse the model_color/model_colorBlend pipeline|- 该方案会保留原有光照结果（在 lightingStage 之后叠加），避免"纯色贴片"式的突兀效果|- This approach preserves the original lighting result (applied after lightingStage), avoiding abrupt "solid color patch" effects.
api/types/OrbitState.md|轨道全状态输出（满足"同时包含 ECI/ECEF/LLA/时间戳"的约束）。|Full orbit state output (satisfying the constraint of including ECI/ECEF/LLA/timestamp simultaneously).|- `eci.frame` 固定为 `TEME`（与 SGP4 输出一致）|- `eci.frame` is always `TEME` (consistent with SGP4 output)|- `ecef.frame` 固定为 `ECEF`|- `ecef.frame` is always `ECEF`|- `lla` 为角度制经纬高（基于 `Daisy.Ellipsoid`）|- `lla` is latitude/longitude/height in degrees (based on `Daisy.Ellipsoid`)|- `enu` 仅在传入 `observer`（或 `includeENU`）时输出|- `enu` is only output when `observer` (or `includeENU`) is provided
api/types/OrbitStateECEF.md|轨道状态（ECEF / 地固坐标）。|Orbit state (ECEF / Earth-fixed coordinates).|单位：|Units:
api/types/OrbitStateECI.md|轨道状态（ECI / TEME）。|Orbit state (ECI / TEME).|单位：|Units:
api/types/OrbitStateENU.md|观测者局部坐标（ENU）以及派生量（方位/仰角/距离）。|Observer local coordinates (ENU) and derived quantities (azimuth/elevation/range).|单位：|Units:
api/types/OrbitStateLLA.md|地理坐标（经纬高，角度制）。|Geographic coordinates (latitude/longitude/height, in degrees).|单位：|Units:
api/types/ParticleEmitterConfig.md|粒子发射器配置。|Particle emitter configuration.|支持 17 种发射器类型：circle、box、cone、sphere、point、line、rectangle、|Supports 17 emitter types: circle, box, cone, sphere, point, line, rectangle,|// 圆形发射器|// Circle emitter|// 锥形发射器|// Cone emitter
api/types/PassOptions.md|过境预报选项。|Pass prediction options.|- `minElevationDeg`: 最小仰角阈值|- `minElevationDeg`: minimum elevation angle threshold|- `stepSeconds`: 采样步长（越小越精细但更耗时）|- `stepSeconds`: sampling step size (smaller = more precise but slower)
api/types/PropagationOptions.md|传播输出选项。|Propagation output options.|- `centralBody`: 用于椭球/几何侧的中心天体选择（默认 earth）|- `centralBody`: central body selection for ellipsoid/geometry (default earth)|- `ellipsoid`: 显式指定椭球（优先级高于 `centralBody`）|- `ellipsoid`: explicitly specify the ellipsoid (higher priority than `centralBody`)|- `earthRotationRateRadPerSec`: 地球自转角速度（用于 ECEF 速度修正项 ω × r），默认 `7.2921150e-5`|- `earthRotationRateRadPerSec`: Earth's rotation rate (used for ECEF velocity correction term ω × r), default `7.2921150e-5`|- `observer`: 指定观测点则可输出 `enu`|- `observer`: specify an observation point to output `enu`|- `includeENU`: 是否强制输出 ENU（设置 `observer` 时无需显式传 true）|- `includeENU`: whether to force ENU output (not needed when `observer` is set)
api/types/PW.BaseObjectModelDistanceFallbackPointOptions.md|兼容字段，等同 `sizePx`。|Compatibility field, equivalent to `sizePx`.|像素尺寸。默认 fallback 点只按像素渲染，不使用米制尺寸。|Pixel size. Default fallback points are rendered in pixels only, not using metric dimensions.
api/types/PW.CelestialEllipsoidOrientation.md|天体局部坐标系到世界坐标的姿态来源|Orientation source from celestial body local coordinates to world coordinates|- 支持固定矩阵/四元数|- Supports fixed matrix/quaternion|- 或按时间返回的函数|- Or a function that returns a value by time
api/types/PW.CelestialEllipsoidPositionECEF.md|天体中心在 ECEF 下的位置来源|Position source of the celestial body center in ECEF|- 可直接给定固定坐标|- Can be given as fixed coordinates|- 可提供按时间返回的函数|- Can be provided as a function returning a value by time
api/types/PW.GroundStationPresetModel.md|GroundStation 配置。|GroundStation configuration.|默认会挂载一个地面站天线模型；如只需要抽象点位，可传 `model: false` 关闭。|By default, a ground station antenna model is mounted. Pass `model: false` to use only an abstract point.
api/types/PW.VesselConfig.md|Vessel 配置。|Vessel configuration.|目前复用 VehicleConfig，后续可扩展船舶专有字段（例如航线、航速模型、船体参数等）。|Currently reuses VehicleConfig; can be extended with vessel-specific fields (e.g., routes, speed models, hull parameters) in the future.
api/types/TextureCoordinatesHoles.md|纹理坐标孔洞结构（positions + holes）。|Texture coordinate hole structure (positions + holes).|与 `Holes` 对应，但 positions 为 `Daisy.Cartesian2[]`。|Corresponds to `Holes`, but positions are `Daisy.Cartesian2[]`.
api/types/TimeFormatPreset.md|时间格式预设。|Time format preset.|`utc` 与 `bjt` 输出带时区名称的日期时间，`t0` 输出相对参考时刻的累计秒；|`utc` and `bjt` output date-time with timezone name, `t0` outputs cumulative seconds relative to the reference time;|其余预设用于选择常见的日期、时间或 ISO 风格格式。历史兼容预设仍可使用，|Other presets select common date, time, or ISO-style formats. Legacy compatibility presets still work,|新代码建议优先选择语义明确的 `utc`、`bjt` 或具体格式预设。|new code should prefer the semantically clear `utc`, `bjt`, or specific format presets.
api/types/TimeFormatter.md|旧版时间文本格式化函数。|Legacy time text formatting function.|请使用 `TimeFormatConfig` 配置时间格式。|Use `TimeFormatConfig` to configure time formatting.
api/types/TimeInterpolationMode.md|离散采样序列的插值模式。|Interpolation mode for discrete sample sequences.|- `"step"`：阶跃（适合开关量/枚举类，区间内保持前值）|- `"step"`: step (suitable for boolean/enum types, holds the previous value within the interval)|- `"linear"`：线性插值（默认，稳定不易过冲）|- `"linear"`: linear interpolation (default, stable, no overshoot)|- `"cubic"`：三次平滑插值（更平滑，适合连续参数）|- `"cubic"`: cubic smooth interpolation (smoother, suitable for continuous parameters)
api/types/TimeSeries.md|时间采样序列。|Time sample series.|- `samples`：离散采样点列表（会按 time 排序）|- `samples`: list of discrete sample points (sorted by time)|- `interpolation`：插值模式|- `interpolation`: interpolation mode
api/types/TimeValue.md|兼容别名：历史上很多 options 字段使用 TimeValue 命名。|Compatibility alias: historically many options fields used the TimeValue name.|建议新代码优先使用 `SimTimeValue<T>`，表达"仿真时间驱动"的语义更明确。|New code should prefer `SimTimeValue<T>`, which more clearly expresses "simulation time-driven" semantics.
api/types/VisibilityStrategy.md|可见性策略配置。|Visibility strategy configuration.|可见性控制模式。|Visibility control mode.
api/types/VisibilityWindow.md|可视窗口（用于互视/遮挡分析）。|Visibility window (for inter-satellite visibility/occlusion analysis).|- `startUtc`/`endUtc` 为 UTC|- `startUtc`/`endUtc` are UTC|- `satIds` 为参与窗口的卫星标识（当前实现为 NORAD ID 字符串）|- `satIds` are satellite identifiers participating in the window (currently NORAD ID strings)
api/variables/index.md|共 89 个 variables。|89 variables total.| | 名称 | 链接 | | Name | Link |
api/variables/resolveTimeValue.md|兼容导出：历史 API 名称。|Compatibility export: legacy API name.|解析仿真时间值。|Resolves a simulation time value.
api/variables/ShaderParams.md|Shader 动态参数快捷构造器。|Shader dynamic parameter shortcut constructor.|返回值可直接放入 `MaterialFactory.Custom()` 的 `uniforms`；材质绘制时会读取最新值。|The return value can be directly placed in `MaterialFactory.Custom()`'s `uniforms`; the material reads the latest values during rendering.