api/classes/BoxShapeEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/CircleShapeEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/ConeShapeEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/CrownEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/CubeGeometry.md||Daisy圆锥/柱体（封装 CubeGeometry）||计算矩形底面边界射线与椭球的交点（投影 footprint）||与 EllipticConeGeometry 的 computeBeamFootprintOnGround 风格一致。||获取用于2D绘图的 footprint 投影坐标（Cartesian3），基于矩形底面
api/classes/CylinderEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/DaisyGLSLError.md||Daisy GLSL 转换错误，包含错误标识符在源码中的行列信息。||错误所在列，从 1 开始。||错误所在行，从 1 开始。
api/classes/DiamondEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/HemisphereEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/LineEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/OrbitEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/PointEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/RectangleEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/RingEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/SphereShapeEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/SpiralEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/TorusEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/VibrationEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/classes/WaveEmitter.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/functions/resolveSimTimeDerivative.md||计算仿真时间值在指定时刻的“变化率”（导数，单位：每秒）。||说明：||- 仅对“离散采样”输入（TimeSample/TimeSeries）有意义||- 对 `"step"` 会返回 0（区间内保持常量）||- 采用数值微分近似：在 `time` 左右取一个小时间窗做对称差分||// rate: number | undefined，表示 range 每秒变化量
api/functions/toCesiumColor.md||将 Daisy 颜色输入转换为内部颜色值。||Daisy 颜色对象或 CSS 颜色字符串，例如 `#ff0`、`#ffff00`、`rgba(...)`。||当 `color` 为空时返回的默认值。
api/functions/toCesiumMaterial.md||将 Daisy 材质输入转换为内部渲染材质实例。||- 字符串：作为图片 URL 创建图片材质||- 材质实例：复制为独立实例，避免共享对象被销毁||- Daisy 颜色对象：创建纯色材质||材质描述
api/interfaces/CelestialSunOcclusionOptions.md||天体太阳遮挡判定参数。||遮挡天体。默认使用地球模型。||光源天体。默认使用太阳模型。
api/interfaces/DaisyMaterialShaderSource.md||由旧版显式包装 API 创建的 Daisy 材质 Shader 源码描述。||Daisy 材质源码描述的稳定识别标记。||待转换的 Daisy GLSL 源码。
api/interfaces/EntityPositionShaderParamOptions.md||实体位置动态参数配置。||实体位置不可用时的回退世界坐标。默认零向量。||位置不可用时的处理策略。默认 `hold`。
api/interfaces/ParticleEmitterLike.md||粒子发射器接口。||发射一个粒子。||待初始化的粒子对象（需写入 position 和 velocity）。
api/interfaces/ParticleLike.md||粒子接口（描述单个粒子的位置与速度）。||粒子位置（世界坐标）。||粒子速度向量。
api/interfaces/TimeDynamicPropertyOptions.md||通用动态属性配置。||未命中任何区间时返回的默认值。||初始时间区间列表；构造时会复制数组。
api/types/ArcRotateCameraViewForm.md||环绕视角参数（球坐标 + roll）。||约定：||- theta：水平角（绕局部 Z 轴）||- phi：俯仰角（限制在接近 ±90° 以内，避免奇异）||- radius：距离（大于 0）||- roll：绕视线方向滚转
api/types/ArcRotateDaisyEntityLike.md||可作为环绕目标的 DaisyEntity 兼容结构。||说明：||- worldMatrix/getWorldMatrix/getCurrentMatrix/getMatrix/matrix 任意提供一种即可||- 当 worldMatrix 不可用时，可通过 getState(time) 的 position 字段回退生成平移矩阵
api/types/ArcRotateEntityLike.md||可作为环绕目标的“兼容对象”（偏底层风格）。||最少需要：||- worldMatrix：目标在世界坐标系下的变换
api/types/CameraViewTarget.md||相机视图/飞行的目标描述。||支持多种输入形式，内部会尽可能转换为三维坐标：||- `Entity` 或 `Entity[]`：取实体当前位置||- `Cartesian3`/`Cartographic` 或其数组：直接使用||- `{x,y,z}`：按世界坐标（米）||- `{lon,lat,height}` / `{lng,lat,alt}` / `{longitude,latitude,height}`：按经纬度（度）与高度（米）||- `[lon,lat]` / `[lon,lat,height]`：按经纬度（度）与高度（米）||- `Promise`：异步解析后再执行
api/types/DataSourceOptions.md||数据源统一配置入口。||数据源||数据源类型
api/types/LabelOptionsWithFormat.md||带格式化回调的标签选项。||显示距离条件。||自定义文本格式化函数。
api/types/ModelAnimationInfo.md||模型动画信息。||动画索引。||动画名称。
api/types/ModelDistanceFallbackPointOptions.md||模型远距离降级点配置。||是否启用远距离降级点。||远距离降级距离倍数。
api/types/ModelNodeInfo.md||模型节点信息。||节点 ID。||节点名称。
api/types/PW.BaseObjectTransformTimeline.md||物理对象的局部变换时间采样配置。||说明：||- 支持常量、回调、采样数组、采样序列四种 `TimeValue` 形式||- `translate` 为推荐字段名||- `tanslate` 为历史兼容拼写
api/types/PW.GroundStationConfig.md||天线节点控制配置。||预设站型模型。||- 不传：默认 dsn34||- false：不自动挂载模型||- ModelOptions：使用自定义模型
api/types/PW.LinkTimeRange.md||链路显示时间区间，包含开始时刻、不包含结束时刻。||区间结束时刻。||区间开始时刻。
api/types/PW.OrbitElementsViewComponentOptions.md||启用轨道元素视图时自动设置的地球透明度（0~1），默认 0.35。||设为 1 则不透明（不修改地球透明度）。||赤道面填充色，默认半透明黄色||轨道面填充色，默认半透明青色
api/types/PW.SensorApertureDeg.md||传感器单轴孔径角或横纵两个方向的动态孔径角，单位为度。||横向孔径角。||纵向孔径角。
api/types/SimTimeValue.md||仿真时间值（推荐使用的语义名）。||支持四种输入方式：||- 常量：`T`||- 回调：`(time) => T`||- 采样数组：`TimeSample<T>[]`||- 采样对象：`TimeSeries<T>`||// 1) 常量||// 2) 回调||// 3) 采样数组（默认 linear）||// 4) 采样对象（显式 cubic）
api/types/SolidMaterialOptions.md||纯色材质配置。||覆盖颜色自身透明度的 alpha 值，范围为 0 至 1。||基础颜色。默认白色。
api/types/TimePosition.md||单个带时间戳的位置点。||xyz 坐标 单位米||时间戳精确到毫秒
api/variables/Spg4.md||SGP4 工具单例。||提供：||- TLE 获取（CelesTrak）与本地缓存||- 轨道源归一化（TLE / OMM XML / JSON GP）||- 基于 `jspredict-dc` 的 SGP4 传播（位置/星历/过境）