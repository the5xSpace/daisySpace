api/classes/LiteController.md|精简模式控制器。|仅包含浮动控制面板，不包含键盘快捷键等高级控制组件。|适用于对 UI 复杂度要求较低的场景。|创建精简控制器。|DaisyUIManager 实例。|挂载 DOM 容器。|控制器根 DOM 元素。|销毁控制器并释放资源。
api/classes/SimulationTimeWidget.md|独立的仿真时间显示控件，仅显示当前仿真时间。|- 支持 preset 位置预设（与控制面板一致）|- 支持任意 position 覆盖（left/top/right/bottom/transform）|- 支持亮/暗主题、圆角、边框与背景透明度、额外 className|创建并挂载仿真时间控件，同时开始监听仿真时钟。|控件根节点。|解除时钟监听、拖拽和层级管理，并移除控件节点。|立即按当前仿真时刻刷新显示文本。|更新标题；传入空字符串会隐藏标题节点。
api/classes/StandardController.md|标准模式控制器。|包含浮动控制面板和键盘快捷键控制组件。|适用于需要完整交互控制的场景。|创建标准控制器。|DaisyUIManager 实例。|挂载 DOM 容器。|控制器根 DOM 元素。|销毁控制器并释放资源。
api/interfaces/CapsuleParticleFrameMetadata.md|本帧真实有 alpha 的内容包围盒，单位：sourceSize 像素坐标。|实际裁剪区域，单位：sourceSize 像素坐标。为了动画稳定，当前同一批帧共用 union crop。|原始逻辑绘制区域。length/radius 仍以这个区域作为基准。|裁剪 sprite 对应的局部播放矩形。|x/width 沿喷焰长度方向，0 是喷口，1 是 length 末端；|y/height 沿横向宽度方向，-1 到 1 对应完整 radius 范围。|粒子实际生成的大画布尺寸，单位：px。|裁剪后真正送入材质播放的 sprite 尺寸，单位：px。
api/interfaces/MarkerTarget.md|标记目标配置。|定义一个需要在场景中标注的目标点。|标记颜色。|获取目标在指定时刻的位置。|仿真时间。|目标位置（世界坐标）。|标记标签文本。
api/interfaces/SpiralFlowMaterialOptions.md|基础颜色。默认蓝色。|归一化环带密度，范围为 0 至 1。默认 `0.3`。|流动方向；`-1` 向上，其余值向下。默认向下。|整体透明度，范围为 0 至 1。默认 `1`。|流动速度，最小值为 `0.1`。默认 `1`。|螺旋环带颜色。默认黄色。|环带宽度，范围为 0.05 至 0.95。默认 `0.35`。
api/types/ArcRotateCameraOptions.md|环绕相机配置项。|- viewForm 优先级高于 initialXXX|- localBodyAxis 可用于覆盖目标的局部坐标系（用于定义“绕谁转”的参考轴）|当目标离椭球地表低于该高度（米）时，自动禁用触地绕行策略。|近地目标继续使用触地绕行容易把相机压进地表，设置该阈值后会改为阻止继续下拉。|是否允许相机视线触地后压缩有效半径，形成“贴地绕行/从底部绕过”的交互。|关闭后，继续向地面方向拖拽会被阻止，但水平旋转、向上拖拽和缩放仍可继续。|跟随参考系模式。
api/types/CentralBody.md|基于 `wasm-sgp4` 内核的高精度轨道分析器（JS/TS 层只做“分析 + 坐标/椭球对齐”）。|设计原则：|- 轨道动力学（SGP4 传播）全部在 `wasm-sgp4` 中完成|- JS/TS 层负责：时间统一（UTC）、坐标系转换（）、椭球几何、分析逻辑（过境/交汇/互视等）|坐标系约定：|- ECI：这里使用 `TEME`（与 SGP4 输出一致）|- ECEF：使用 的 `TEME -> PseudoFixed` 变换矩阵（见 `Daisy.Transforms.computeTemeToPseudoFi|- LLA：使用 `Daisy.Ellipsoid.cartesianToCartographic` 输出经纬高（角度制）|- ENU：使用 `Daisy.Transforms.eastNorthUpToFixedFrame` 基于椭球建立局部 ENU|时间系统约定：|- 所有输入与输出时间均为 UTC（JS Date 的 epoch 毫秒 + `timeBasis: "UTC"` 标识）
api/types/CubeGeometryOptions.md|圆锥体几何参数|底部长（单位：米）|底部宽（单位：米）|高度（单位：米）|顶面长（单位：米）|顶面宽（单位：米）|顶点格式。
api/types/DaisyCellularOptions.md|细胞噪声材质配置。|单元内部颜色。|单元数量。|单元边缘颜色。|单元边缘宽度。|单元随机扰动强度。|整体透明度。
api/types/DaisyContourBandsOptions.md|分层等值带材质配置。|色带数量。|高值颜色。|分界线宽度。|低值颜色。|整体透明度。|色带倾斜偏移。
api/types/DaisyGridGlowOptions.md|发光网格材质配置。|网格背景颜色。|横纵网格数量。|网格线颜色。|网格线宽度。|整体透明度。|发光脉冲速度。
api/types/DaisyNoiseFieldOptions.md|噪声场材质配置。|噪声低值颜色。|噪声高值颜色。|明暗对比度，范围为 0 至 1。|整体透明度，范围为 0 至 1。|噪声空间尺度。|动画速度。
api/types/DaisyScanlineOptions.md|扫描线材质配置。|背景颜色。|扫描线密度。|整体透明度。|扫描线颜色。|扫描速度。|扫描线宽度。
api/types/DaisyTurbulenceOptions.md|湍流材质配置。|湍流低值颜色。|湍流高值颜色。|扰动强度。|整体透明度。|湍流空间尺度。|动画速度。
api/types/GeoEarthTransparencyOptions.md|统一透明度，0 为完全透明，1 为完全不透明。|背面透明度。|背面透明度随相机距离变化。|是否启用地球半透明。|正面透明度。|正面透明度随相机距离变化。|是否显示大气层。
api/types/PW.SensorLinkOptions.md|传感器链路跟踪与流动效果配置。|链路中的波束流动效果配置。|推荐写法：|上述写法会直接使用内置默认流动材质；|如果需要覆盖默认表现，再补充 `material` 或其它参数即可。|链路跟踪计划（可按时段切换目标）。|推荐写法：|- 只写 `track` 即可完成基础跟踪|- 当你只需要“默认流动效果”时，`flow` 也可以只写|- 默认材质会自动根据波束颜色生成，视觉效果通常已经足够好