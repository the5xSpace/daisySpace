api/classes/index.md|||共 234 个 classes。|||| 名称 | 链接 |
api/classes/ModelNodeTransform.md|||节点变换控制器（链式调用）。|||通过 ModelFeature.transformNode(name) 获取实例；变换会作为状态副作用在每帧 update 自动生效。
api/enums/index.md|||共 27 个 enums。|||| 名称 | 链接 |
api/functions/classifySunOcclusion.md|||根据观察点看到的光源与遮挡天体视圆关系判定光照状态。|||当遮挡天体视圆完整覆盖光源时返回 `umbra`，部分相交时返回 `penumbra`，|||否则返回 `sunlit`。
api/functions/createDisposableDetachMaterial.md|||创建一个组件移除时使用的临时材质。|||线对象从集合移除时会销毁自身挂载的材质。Feature 销毁只应解除组件对业务材质的引用，|||不能销毁外部传入或多处共享的材质。
api/functions/encodePolygons.md|||将所有 CoveragePolygon 绘制到一张 canvas 上。|||每个 ring 会先做经线解包，再展开成一个或多个独立的 canvas path，|||这样就不会把跨 ±180° 的边硬连成一条横贯全图的长边。
api/functions/formatDateTime.md|||使用 token 模式格式化日期时间上下文。|||支持 `YYYY`、`MMM`、`MM`、`M`、`DD`、`D`、`HH`、`H`、`mm`、`m`、|||`ss`、`s`、`SSS`、`TZ` 和 `Z`。
api/functions/hslToRgb.md|||HSL 转 RGB。|||- h: 角度制 0~360
api/functions/index.md|||共 86 个 functions。|||| 名称 | 链接 |
api/functions/mergeTimeFormatOptions.md|||合并基础与局部时间格式配置。|||当局部配置是预设名或回调时会完整替换基础配置；局部配置为对象时按字段覆盖。
api/functions/rgbToHsl.md|||RGB 转 HSL。|||- 输入与输出分量均为 0~1，h 为角度制 0~360
api/interfaces/BodyTrackedCameraState.md|||机体跟踪相机的局部状态。|||将相机位置与姿态表示在“机体坐标系”（随天体自转的局部坐标系）中，便于在天体旋转时保持相对视角稳定。
api/interfaces/DynamicShaderParamOptions.md|||自定义动态 Shader 参数配置。|||解析器返回 `undefined` 时使用的值，同时用于校验声明的 GLSL 类型。
api/interfaces/index.md|||共 129 个 interfaces。|||| 名称 | 链接 |
api/interfaces/UI.PopoverVisibilityChangeEvent.md|||当前帧 DOM 是否真实显示；距离、遮挡、离屏可能让它为 false。|||由触发器/API 决定的“请求显示”状态。
api/modules/index.md|||共 23 个 modules。|||| 名称 | 链接 |
api/types/ArcRotateInputInstallOptions.md|||输入监听安装参数。|||- canvas：接收 pointer/wheel 事件|||- screenSpaceCameraController：可选，用于在安装时禁用输入、卸载时恢复
api/types/ArcRotateTargetFrameMode.md|||环绕相机跟随目标时使用的参考系。|||- model：使用目标 worldMatrix，环绕视角会继承目标姿态|||- enu：使用目标位置的 East-North-Up 参考系，避免相机继承目标俯仰/滚转
api/types/ArrowPointerLabelOptions.md|||箭头前端 label 配置。|||该类型在 `DaisyLabelOptions` 的基础上要求必须提供 `text`。
api/types/ArrowPointerTargetCallback.md|||ArrowPointerFeature 可用的指向目标类型。|||- `Daisy.Cartesian3`：静态坐标点|||- `Entity`：Daisy 实体（内部会转成回调，每帧取其当前位置）|||- `Entity`： 实体（内部会转成回调，每帧用 `position.getValue(time)` / `getValueInReferenceFrame`）|||- `ArrowPointerTargetCallback`：回调函数（由使用方自行计算目标位置）
api/types/AutoNormalsMode.md|||法线自动计算模式。|||- "flat"：每个顶点的法线 = 其所属三角形的面法线，棱角分明。|||- "smooth"：每个顶点的法线 = 其相邻三角形面法线的平均，光滑曲面。|||- false：不自动计算，使用用户传入的法线。
api/types/CollisionHandlerFunc.md|||碰撞处理回调函数类型|||被碰撞的盒子，可以通过 box.entity 获取实体对象
api/types/ConjunctionEvent.md|||交汇事件（>=2 星最近距离预报）。|||- `tcaUtc`: 最近距离发生时刻（TCA）|||- `minDistance`: 最近距离（m）|||- `relativeSpeedPerSecond`: 相对速度模长（m/s），基于 ECEF 速度估计|||- `stateA`/`stateB`: 同一 `tcaUtc` 下双方的完整状态输出（含 ECI/ECEF/LLA）
api/types/ConjunctionOptions.md|||交汇预报选项。|||- `stepSeconds`: 采样步长（越小越精细但更耗时）|||- `distanceThreshold`: 触发候选区间的距离阈值（只对阈值内区间做细化搜索）
api/types/EphemerisDataCallbackOptions.md|||星历数据源配置（回调追加形式）。|||用于持续追加位置点（如实时流数据）。
api/types/FeatureEventHandleHost.md|||FeatureEventHandle 的宿主能力集合。|||由 Feature 基类实现，用于把 ViewerEventHandle 的 SPACE_ENTITY_* 事件|||映射为“Feature 自身可监听 + 可选向上提交 Entity”的统一交互事件系统。
api/types/Holes.md|||多边形孔洞结构（positions + holes）。|||与 Daisy.PolygonHierarchy 的概念一致，可递归嵌套。
api/types/index.md|||共 266 个 types。|||| 名称 | 链接 |
api/types/InterSatelliteVisibilityOptions.md|||互视窗口预报选项。|||- `stepSeconds`: 采样步长（越小越精细但更耗时）
api/types/KeplerElementDataOptions.md|||开普勒根数数据源配置。|||数值单位与含义取决于上游数据约定，SDK 会按字段名直接使用。
api/types/ModelNodeTransformState.md|||模型节点变换状态（状态副作用）。|||- 该状态不会立即修改节点渲染，而是在 ModelFeature.update() 每帧自动应用到对应节点|||- matrix 优先级最高：|||- matrix === null：恢复为 undefined，把变换控制权交还给 glTF 原始 transform/动画|||- matrix 存在：直接覆盖节点 matrix|||- 底层 ModelNode 本身不提供节点级 color/alpha|||- Daisy 在内部通过修改模型 DrawCommand 的 uniformMap，让节点复用 的 model_color/model_colorBlend 管线|||- 该方案会保留原有光照结果（在 lightingStage 之后叠加），避免“纯色贴片”式的突兀效果
api/types/OrbitState.md|||轨道全状态输出（满足“同时包含 ECI/ECEF/LLA/时间戳”的约束）。|||- `eci.frame` 固定为 `TEME`（与 SGP4 输出一致）|||- `ecef.frame` 固定为 `ECEF`|||- `lla` 为角度制经纬高（基于 `Daisy.Ellipsoid`）|||- `enu` 仅在传入 `observer`（或 `includeENU`）时输出
api/types/OrbitStateECEF.md|||轨道状态（ECEF / 地固坐标）。|||单位：
api/types/OrbitStateECI.md|||轨道状态（ECI / TEME）。|||单位：
api/types/OrbitStateENU.md|||观测者局部坐标（ENU）以及派生量（方位/仰角/距离）。|||单位：
api/types/OrbitStateLLA.md|||地理坐标（经纬高，角度制）。|||单位：
api/types/ParticleEmitterConfig.md|||粒子发射器配置。|||支持 17 种发射器类型：circle、box、cone、sphere、point、line、rectangle、|||// 圆形发射器|||// 锥形发射器
api/types/PassOptions.md|||过境预报选项。|||- `minElevationDeg`: 最小仰角阈值|||- `stepSeconds`: 采样步长（越小越精细但更耗时）
api/types/PropagationOptions.md|||传播输出选项。|||- `centralBody`: 用于椭球/几何侧的中心天体选择（默认 earth）|||- `ellipsoid`: 显式指定椭球（优先级高于 `centralBody`）|||- `earthRotationRateRadPerSec`: 地球自转角速度（用于 ECEF 速度修正项 \(\omega \times r\)），默认 `7.2921150e-5`|||- `observer`: 指定观测点则可输出 `enu`|||- `includeENU`: 是否强制输出 ENU（设置 `observer` 时无需显式传 true）
api/types/PW.BaseObjectModelDistanceFallbackPointOptions.md|||兼容字段，等同 `sizePx`。|||像素尺寸。默认 fallback 点只按像素渲染，不使用米制尺寸。
api/types/PW.CelestialEllipsoidOrientation.md|||天体局部坐标系到世界坐标的姿态来源|||- 支持固定矩阵/四元数|||- 或按时间返回的函数
api/types/PW.CelestialEllipsoidPositionECEF.md|||天体中心在 ECEF 下的位置来源|||- 可直接给定固定坐标|||- 可提供按时间返回的函数
api/types/PW.GroundStationPresetModel.md|||GroundStation 配置。|||默认会挂载一个地面站天线模型；如只需要抽象点位，可传 `model: false` 关闭。
api/types/PW.VesselConfig.md|||Vessel 配置。|||目前复用 VehicleConfig，后续可扩展船舶专有字段（例如航线、航速模型、船体参数等）。
api/types/TextureCoordinatesHoles.md|||纹理坐标孔洞结构（positions + holes）。|||与 `Holes` 对应，但 positions 为 `Daisy.Cartesian2[]`。
api/types/TimeFormatPreset.md|||时间格式预设。|||`utc` 与 `bjt` 输出带时区名称的日期时间，`t0` 输出相对参考时刻的累计秒；|||其余预设用于选择常见的日期、时间或 ISO 风格格式。历史兼容预设仍可使用，|||新代码建议优先选择语义明确的 `utc`、`bjt` 或具体格式预设。
api/types/TimeFormatter.md|||旧版时间文本格式化函数。|||请使用 `TimeFormatConfig` 配置时间格式。
api/types/TimeInterpolationMode.md|||离散采样序列的插值模式。|||- `"step"`：阶跃（适合开关量/枚举类，区间内保持前值）|||- `"linear"`：线性插值（默认，稳定不易过冲）|||- `"cubic"`：三次平滑插值（更平滑，适合连续参数）
api/types/TimeSeries.md|||时间采样序列。|||- `samples`：离散采样点列表（会按 time 排序）|||- `interpolation`：插值模式
api/types/TimeValue.md|||兼容别名：历史上很多 options 字段使用 TimeValue 命名。|||建议新代码优先使用 `SimTimeValue<T>`，表达“仿真时间驱动”的语义更明确。
api/types/VisibilityStrategy.md|||可见性策略配置。|||可见性控制模式。
api/types/VisibilityWindow.md|||可视窗口（用于互视/遮挡分析）。|||- `startUtc`/`endUtc` 为 UTC|||- `satIds` 为参与窗口的卫星标识（当前实现为 NORAD ID 字符串）
api/variables/index.md|||共 89 个 variables。|||| 名称 | 链接 |
api/variables/resolveTimeValue.md|||兼容导出：历史 API 名称。|||解析仿真时间值。
api/variables/ShaderParams.md|||Shader 动态参数快捷构造器。|||返回值可直接放入 `MaterialFactory.Custom()` 的 `uniforms`；材质绘制时会读取最新值。