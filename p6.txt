api/classes/PW.Atmosphere.md||行星大气参数容器||- 封装 Rayleigh/Mie 散射、吸收与强度等物理参数||- 提供 getUniforms 将参数转换为材质 uniforms，供渲染使用||- 与 CelestialAtmosphereFeature 搭配，在场景中显示大气层散射效果||// 定义火星大气参数||// 绑定到特性并显示||构造函数||大气物理参数||将大气物理参数转换为材质 uniforms||可直接用于 Daisy.Material fabric.uniforms 的对象
api/enums/DataSource.md||轨道/位置数据的来源类型。||该枚举通常用于区分 TLE、开普勒根数、星历采样等不同的数据输入方式。||星历数据||kepler元素||实时更新位置||tle 必须包含3行元素
api/enums/ViewScene.md||内置视距场景模板名。||不同场景对应不同的默认显示距离阈值配置（例如航天/航空/海事/地面）。||航空尺度||地面/近景尺度||海事/海面尺度||航天/空间尺度
api/interfaces/DaisyGeometryDescriptor.md||Daisy 几何描述 —— FreeGeometryFeature 的核心输入。||positions 为局部坐标（相对 Entity 原点）。||包围球（可选，缺失时从 positions 自动计算）||索引数据（必填）||顶点法线（可选，缺失时按 autoNormals 自动计算）||顶点位置（必填，局部坐标）||纹理坐标（可选）
api/interfaces/DownEmitDiffuseMaterialOptions.md||底部扩散环颜色。默认亮绿色。||光束基础颜色。默认绿色。||扩散环半径，范围为 0.05 至 1。默认 `0.45`。||扩散环宽度，范围为 0.01 至 0.5。默认 `0.06`。||整体透明度，范围为 0 至 1。默认 `1`。||下落动画速度，最小值为 `0.1`。默认 `1`。
api/interfaces/FeaturePickedEvent.md||拾取到的组件/要素标识（可能包含 `__` 分隔信息）||组件类型（如 `Entity` 或 Feature 的 type）||Daisy 实体对象||Daisy 实体 ID||可选：当拾取到模型节点时，提供 glTF 节点索引||可选：当拾取到模型节点时，提供节点名
api/interfaces/GeoFogOptions.md||场景雾效配置；仅修改传入的字段。||雾效亮度。||雾密度；数值越大，远处地表越早融入背景。||是否启用雾效。||是否直接渲染雾色；关闭后仍可保留雾效对细节层级的影响。||雾效参与细节层级计算时使用的屏幕空间误差系数。
api/interfaces/NeonScanMaterialOptions.md||扫描带宽度，范围为 0.01 至 0.5。默认 `0.15`。||背景颜色。默认紫色。||柔光范围，范围为 0.05 至 0.95。默认 `0.35`。||霓虹扫描带颜色。默认青色。||整体透明度，范围为 0 至 1。默认 `1`。||扫描速度，最小值为 `0.1`。默认 `1.5`。
api/interfaces/ParticleScreenSpaceLimitOptions.md||是否启用屏幕空间预算。传入 false 时即使配置对象存在也不介入。||发射率上限。与 targetParticleSpacingPx 同时存在时取更严格者。||单颗粒子最终 billboard 尺寸上限（像素）。仅影响 sizeInMeters=false 的屏幕像素粒子。||尾焰在屏幕上的最大长度（像素）。通过动态压缩粒子生命周期并裁剪超长存活粒子实现。||发射率下限。避免尾焰被压得过稀。||期望粒子沿尾焰方向的屏幕间距（像素）。用于动态限制发射率，避免近距离变成粒子云。
api/interfaces/PW.RouteWaypoint.md||单个航点定义||自定义图标 URL（不传则使用内置标记图标）||主标题（显示在标签第一行）||Popover 内容（HTML 字符串），不传则不创建 Popover||航点世界坐标（WGS-84）||副标题（显示在标签第二行，更小的字号）
api/interfaces/RadialPulseMaterialOptions.md||归一化纹理坐标中的扩散中心。默认 `(0.5, 0.5)`。||背景颜色。默认青色。||整体透明度，范围为 0 至 1。默认 `1`。||脉冲环颜色。默认白色。||脉冲环宽度，范围为 0.01 至 0.5。默认 `0.08`。||扩散速度，最小值为 `0.1`。默认 `1`。
api/interfaces/SunConeDimensions.md||日锥物理尺寸。||输入的遮挡天体半径。||输入的两天体中心距离。||在本影尖端轴向位置处的半影外半径。||输入的光源天体半径。||从遮挡天体中心沿背光方向到本影尖端的距离。
api/interfaces/SunOcclusionInput.md||太阳遮挡判定参数。||遮挡天体中心的世界坐标。||遮挡天体半径；单位须与世界坐标一致。||待判定观察点的世界坐标。||光源天体中心的世界坐标。||光源天体半径；单位须与世界坐标一致。
api/interfaces/TaskStepListState.md||自定义任务步骤列表渲染器接收的完整状态。||当前仿真时刻。||触发任务点击行为；未配置点击回调时不存在。||当前场景的仿真结束时刻。||当前场景的仿真开始时刻。||按调度器顺序生成的任务步骤。
api/types/DaisyMaterialDescriptor.md||Daisy 材质描述，可直接传给支持 `DMaterial` 的要素或组件。||材质描述的稳定识别标记。||自定义材质源码；未设置时按已注册材质类型解析。||是否按半透明材质处理。默认 `true`。||内置或已注册的材质类型标识。||材质 uniform 参数。
api/types/GridMaterialOptions.md||网格材质配置。||网格单元内部透明度。默认 `0.1`。||网格线颜色。默认白色。||横纵方向的网格数量；数字表示两个方向使用相同值。默认 8。||横纵方向的网格线偏移；数字表示两个方向使用相同值。默认 0。||横纵方向的网格线粗细；数字表示两个方向使用相同值。默认 1。
api/types/Plane.PlaneGridStyle.md||网格单元填充透明度（0~1）。||目标网格单元的屏幕像素尺寸（仅 followCamera=true 生效）。||是否根据相机距离自动调整网格密度，使屏幕上网格视觉密度更稳定。||网格线宽（像素）。||网格单元最大尺寸（单位：米），用于限制过稀（仅 followCamera=true 生效）。||网格单元最小尺寸（单位：米），用于限制过密导致闪烁（仅 followCamera=true 生效）。
api/types/PW.GeoFencePoint.md||地理围栏点（经纬高）。||单位：||- `lon/lat`：度||- `height`：米||高度，单位为米。||纬度，单位为度。||经度，单位为度。
api/types/PW.GroundStationAntennaPointingOptions.md||方位旋转轴（模型节点局部坐标）。||DSN 预设模型使用 Y 轴作为模型竖直轴。||方位旋转节点。DSN 34m/70m 模型中通常为 `azimuth`。||模型节点与业务角之间的固定偏置。||俯仰旋转轴（模型节点局部坐标）。||俯仰旋转节点。DSN 34m/70m 模型中通常为 `elevation`。
api/types/PW.RocketConfig.md||主动段弹道输入。传入后 Rocket 会在构造或绑定后生成 trajectory。||是否自动把竖直发射模型的本体 +Z 轴安装到 Rocket 前向 +X 轴。||许多火箭/导弹模型在 glTF 内以 +Z 作为鼻锥方向，而 Daisy Vehicle||约定本体前向为 +X。开启后会给模型附加绕本体 Y 轴 -90° 的安装俯仰。||是否根据轨迹速度自动写入姿态。||机体坐标轴调试显示。传入 true 使用 Rocket 默认轴参数；传入对象则透传给 Entity.setBodyAxis。||轨迹起始时刻。省略时优先使用场景当前时间，未绑定 Engine 时使用当前系统时间。
api/types/PW.TimeRange.md||覆盖采样使用的时间范围。||`start` 的兼容别名。||`start` 的兼容别名。||结束时刻。||`end` 的兼容别名。||开始时刻。
api/types/StripeMaterialOptions.md||条纹材质配置。||偶数条纹颜色。默认白色。||奇数条纹颜色。默认黑色。||条纹相位偏移。默认 `0`。||条纹方向。默认水平方向。||条纹重复次数。默认 `5`。
api/types/TleData.md||TLE 数据源配置。||开始时间 为空时使用场景结束时间||时间间隔 单位毫秒||norad编号||开始时间 为空时使用场景开始时间||tle数据